import './MetadataEditor.scss'

function MetadataEditor() {
  return (
    <section className="tool-placeholder-page">
      <div className="tool-placeholder-page__content">
        <span className="tool-placeholder-page__eyebrow">Tool</span>
        <h1>Metadata Editor</h1>
        <p>
          This workspace is ready for the future metadata editing flow.
        </p>
      </div>
    </section>
  )
}

export default MetadataEditor
