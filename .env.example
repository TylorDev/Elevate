# Optional development-only override. Releases always use Elevate's managed local database.
DATABASE_URL="file:./prisma/dev.db"

# Optional portable development data directory.
ELEVATE_ENABLE_PORTABLE_MODE="0"
ELEVATE_PORTABLE_DATA_DIR="./.elevate-data"
